package com.autobill.smartpos.debug

import android.app.Application

/**
 * RELEASE build — no-op. Zero code, zero overhead in production.
 */
object DebugTools {
    fun init(app: Application) = Unit
}

