<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only manifest additions — merged with app/src/main/AndroidManifest.xml by AGP.
    FeatureFlagsActivity is registered here so it is absent from release builds entirely.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".debug.FeatureFlagsActivity"
            android:exported="false"
            android:label="Feature Flags"
            android:theme="@style/Theme.SmartPos" />
    </application>
</manifest>

